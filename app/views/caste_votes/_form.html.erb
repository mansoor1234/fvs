<%= form_with(model: caste_vote) do |form| %>
  <% if caste_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(caste_vote.errors.count, "error") %> prohibited this caste_vote from being saved:</h2>

      <ul>
        <% caste_vote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.hidden_field value: caste_vote.polling_id %>
    <%#= hidden_field_tag 'selected', 'none'  %>
  </div>

  <div class="actions">
    <%= form.hidden_field :candidate_id %>
    <%= hidden_field_tag 'selected', 'none' %>
  </div>

  <div class="actions">
    <%= form.hidden_field :voter_id %>
    <%= hidden_field_tag 'selected', 'none' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>